[pytest]
# The canonical suite is the underwriting package. The top-level tests/ + agent.py
# are the superseded thin first slice (kept for reference) — not collected by default.
# Run them explicitly with `pytest tests/` if needed.
testpaths = underwriting/tests
